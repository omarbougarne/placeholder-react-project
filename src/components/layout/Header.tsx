import React from 'react';

const Header: React.FC = () => {
    return (
        <header className="header">
            <h1 className="header-title">My React TypeScript App</h1>
        </header>
    );
};

export default Header;